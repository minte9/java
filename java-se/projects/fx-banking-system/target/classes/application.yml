spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fxbank
    username: fxuser
    password: fxpass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
server:
  port: 8080
